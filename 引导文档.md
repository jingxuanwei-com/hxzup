# 引导文档（AI 专用）

> 本文档及项目中的 `服务端/README.md`、`客户端/README.md`、`引导器/README.md` 均为参考文档。
> 如果 AI 认为文档中的设计不合理、有矛盾或有更好的方案，**请自行重新规划，不必严格遵循文档内容**。

---

## ⚠️ 使用前判断

- 脚手架已初始化完毕（Vue 3 + Quasar 2 + Vite + TypeScript + Pinia + Vue Router），可直接使用
- 如果不需要此脚手架，删除整个 `web/` 目录，改用其他方案实现管理面板

---

## 项目概况

MC 整合包更新系统：服务端（Go）、客户端（Java/Kotlin Agent）、引导器、Web 管理面板。
详见各目录下 README.md。

## 参考代码（`参考代码/` 目录，酌情参考，不要深入阅读）
> 注意省 token，按需查阅关键文件即可，不要通读整个项目。
- `参考代码/HMCL-main/` — HMCL 启动器源码。参考其**加载器安装逻辑**（Forge/Fabric/NeoForge/Quilt 安装流程、版本 JSON 生成、libraries 处理），不要照搬完整启动器实现
- `参考代码/Mcpatch2JavaClient-main/` — 另一个 MC 更新器项目。参考其 **`-javaagent` 启动逻辑和更新流程**，与本项目架构类似

---

## 一、Web 已加入组件

| 技术 | 版本 | 用途 |
|---|---|---|
| Vue 3 | ^3.5 | 响应式 UI 框架 |
| Quasar 2 | ^2.32 | 组件库（QTree、QMenu、QUploader、QTable 等） |
| Vite | (Quasar 内置) | 构建工具 + 开发热更新 |
| TypeScript | ^6.0 | 类型安全 |
| Pinia | ^4.0 | 状态管理 |
| Vue Router | ^5.0 | 文件路由（filename-based routing） |
| vue-i18n | ^11.3 | 国际化 |
| Sass | — | CSS 预处理器 |
| oxlint + oxfmt | — | 代码检查与格式化 |

---

## 二、常用命令

```bash
cd web/
pnpm install       # 安装依赖（必须使用 pnpm）
pnpm dev           # 开发服务器（热更新，默认 localhost:9000）
pnpm build         # 构建生产版本 → dist/
pnpm typecheck     # 类型检查
pnpm lint          # 检查 + 自动修复
pnpm lint:check    # 仅检查，不修复
```

---

## 三、路由规则（filename-based routing）

Quasar 使用**文件路由**，`src/pages/` 下的文件自动映射为路由：

| 文件路径 | 路由 URL |
|---|---|
| `pages/index.vue` | `/` |
| `pages/login.vue` | `/login` |
| `pages/files/index.vue` | `/files` |
| `pages/files/browse.vue` | `/files/browse` |
| `pages/settings.vue` | `/settings` |

> 不需要手动配置 `router/index.ts`，Quasar 会根据文件结构自动生成路由。

---

## 四、部署方式

构建产物 `dist/` 为纯静态文件，由 Nginx 托管：

```nginx
server {
    listen 80;
    server_name mc-admin.example.com;
    root /path/to/web/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;   # SPA 回退
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080;   # API 代理到 Go 后端
    }
}
```

---

## 五、AI 开发注意事项

- 使用 **pnpm**，不要用 npm 或 yarn
- 新建页面直接在 `src/pages/` 下创建 `.vue` 文件，路由自动生成
- 优先使用 **Quasar 内置组件**，不要引入额外 UI 库
- 暗色主题：在 `quasar.config.ts` 中配置 `framework.dark: true`
- TypeScript strict 模式已开启，注意类型标注
- 提交前运行 `pnpm lint`
- `pnpm build` 输出 `dist/`，交给 Nginx 即可
